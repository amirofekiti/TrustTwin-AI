# Experiments

The experiment registry records the executed research branches and their dispositions.

The full research archive contains the historical phase packs and compact verified result files. Large phase artifacts are not duplicated in the public Git repository; their cryptographic identities are retained under `provenance/`.

Negative and rejected experiments remain in the registry by design.
