# Verified results index

This public repository presents the verified headline results in `docs/results_summary.md` and binds the original result ZIPs through `provenance/result_bundles.sha256`.

Large result bundles, raw predictions and feature matrices are intentionally excluded from Git history.

The release archive used during research contains compact verified per-phase summaries and environment/configuration files.
